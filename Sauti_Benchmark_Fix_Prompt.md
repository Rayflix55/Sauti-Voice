The current benchmark implementation in `benchmark/run_benchmark.ts` is not acceptable and needs to be rebuilt. It currently uses a hardcoded object called `BASELINE_TRANSCRIPTS` with fabricated transcripts that were never generated by any real model — this needs to be deleted entirely.

## What to build instead

Rebuild the benchmark to call three REAL models against REAL audio files:

1. **Sahara** — use the existing `transcribeWithSahara()` function in `server/sahara.ts`, calling the live API with `SAHARA_API_KEY` set in `.env`. Remove the silent fake fallback that returns an empty transcript labeled "Local Simulation" — if the API call fails, the script should log a clear error and skip that row, not fabricate a result.

2. **Whisper (hosted via Hugging Face's free Inference API, NOT the paid OpenAI API)** — build a helper function that:
   - Sends a POST request to `https://api-inference.huggingface.co/models/openai/whisper-large-v3`
   - Sends the raw audio file bytes as the request body (Content-Type should match the audio file, e.g. `audio/wav`)
   - Includes header `Authorization: Bearer ${process.env.HF_API_TOKEN}`
   - Parses the returned JSON for the transcript field (typically `{ "text": "..." }`)
   - Times the request (start to response) as the latency metric
   - Handles the model "cold start" case gracefully: if Hugging Face returns a 503 with a message about the model loading, wait a few seconds and retry once or twice before failing, rather than immediately erroring out
   - `HF_API_TOKEN` will be set in `.env`

3. **Vosk (offline, open-source, free)** — build a helper (Python, using the `vosk` package) that:
   - Loads a small pretrained Vosk model (e.g. `vosk-model-small-en-us-0.15`)
   - Transcribes each clip
   - Captures latency the same way

## Required changes to the pipeline

- Real audio files will live in `benchmark/audio/` — I will provide these as actual `.wav` files matching the filenames already in `benchmark/test_clips.csv`.
- The benchmark script should read each filename from `test_clips.csv`, locate the matching audio file in `benchmark/audio/`, and run it through all three models for real — no lookups, no hardcoded transcripts.
- If an audio file listed in the CSV is missing from `benchmark/audio/`, the script should throw a clear error naming the missing file, not silently substitute fake data.
- Keep the existing WER and entity-accuracy calculation functions (`calculateWER`, `checkEntityAccuracy`) — those are fine, they just need to run on real transcripts instead of fabricated ones.
- Update all references (UI labels in `BenchmarkView.tsx`, markdown report generation) from "Google STT" to "Vosk (offline)" or "Vosk" as the third model.
- The final markdown/CSV report output should be generated from actual computed results, and should not claim any number that wasn't derived from a real model call.

## Additional metrics and reporting requirements

Align this benchmark's methodology with Intron's own published benchmarking approach (see github.com/intron-innovation/Intron-Multimodal-Benchmarking for reference):

1. **Add Character Error Rate (CER)** alongside WER. Implement it the same way as `calculateWER` but operating on characters instead of words. CER gives partial credit for near-miss entity spellings (e.g. a name that's 90% correct) that WER penalizes as a full miss.

2. **Add text normalization before scoring.** Before computing WER/CER, lowercase both reference and hypothesis, strip punctuation, and collapse whitespace. Report results using normalized text as the primary number (this matches standard practice and Intron's own methodology).

3. **Break results down by language group, not just one blended average.** Compute and report separate summary rows for: Pidgin-English clips, Yoruba-English clips, and the pure-English control clip — in addition to the overall average. Do not collapse everything into a single number; per-language-group reporting is what makes the comparison credible to judges who know this space.

4. **Report results honestly even if Sahara doesn't win every category.** Do not adjust the test set, scoring method, or which clips are included based on which model performs best. If a real live run shows a model other than Sahara scoring better on a particular clip or language group, report that plainly in the results table and discussion — this is expected and normal (Intron's own published benchmark shows Sahara losing to Azure on macro-average WER), and a fair report is scored higher than a one-sided one.

## Also fix: the language-detection heuristic in `server/sahara.ts`

The current `detectedLang` logic (regex-matching words like "wey", "dey", "mo", "fẹ́" to guess Pidgin vs Yoruba-English) is a rough internal heuristic, not a real language-identification feature of the Sahara API. Rename the field or add a code comment making clear this is an internal approximation for UI labeling only, so it isn't mistaken for something Sahara itself reports. Don't present this heuristic's output as a benchmarked capability anywhere in the report or demo.

## Why this matters

This benchmark is worth 30% of the competition score — the highest-weighted single criterion — specifically because it's judged on "quality and fairness of the benchmark comparison." Submitting fabricated data risks disqualification if inspected, and defeats the purpose of the comparison. Every number in the final report must trace back to a real API/model call on real audio.

Please confirm you've removed `BASELINE_TRANSCRIPTS` and show me the new pipeline calling all three real models before generating any report.
